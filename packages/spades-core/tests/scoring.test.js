import test from "node:test";
import assert from "node:assert/strict";

test("TODO: scores made bids as bid times 10 plus overtricks", () => {
  assert.ok(true);
});

test("TODO: scores missed bids as negative bid times 10", () => {
  assert.ok(true);
});

