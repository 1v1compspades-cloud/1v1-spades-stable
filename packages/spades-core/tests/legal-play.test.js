import test from "node:test";
import assert from "node:assert/strict";

test("TODO: requires following led suit when possible", () => {
  assert.ok(true);
});

test("TODO: prevents leading spades before broken unless only spades remain", () => {
  assert.ok(true);
});

test("TODO: marks spades broken when a spade is played off-suit", () => {
  assert.ok(true);
});

