import test from "node:test";
import assert from "node:assert/strict";

test("TODO: records one bag for each overtrick", () => {
  assert.ok(true);
});

test("TODO: applies bag penalty at the configured threshold", () => {
  assert.ok(true);
});

