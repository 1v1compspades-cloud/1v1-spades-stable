import test from "node:test";
import assert from "node:assert/strict";

test("TODO: completes the match when a player reaches target score after a hand", () => {
  assert.ok(true);
});

test("TODO: continues tied matches at or above target score", () => {
  assert.ok(true);
});

