import test from "node:test";
import assert from "node:assert/strict";

test("TODO: highest led suit wins when no spade is played", () => {
  assert.ok(true);
});

test("TODO: highest spade wins when any spade is played", () => {
  assert.ok(true);
});

