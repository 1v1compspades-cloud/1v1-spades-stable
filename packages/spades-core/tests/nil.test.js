import test from "node:test";
import assert from "node:assert/strict";

test("TODO: awards nil bonus when a nil bidder takes zero tricks", () => {
  assert.ok(true);
});

test("TODO: applies nil penalty when a nil bidder takes any trick", () => {
  assert.ok(true);
});

