import test from "node:test";
import assert from "node:assert/strict";

test("TODO: creates a standard 52-card 1v1 Spades deck", () => {
  assert.ok(true);
});

test("TODO: deals 13 hidden cards to each player and preserves undealt stock", () => {
  assert.ok(true);
});

