import test from "node:test";
import assert from "node:assert/strict";

test("TODO: accepts bids from 0 through 13", () => {
  assert.ok(true);
});

test("TODO: treats bid 0 as nil", () => {
  assert.ok(true);
});

test("TODO: locks both bids before trick play starts", () => {
  assert.ok(true);
});

